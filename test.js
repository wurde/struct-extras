'use strict'

/**
 * Dependencies
 */

const assert = require('assert')
const struct = require('./index')

/**
 * Assertions
 */

describe("struct", () => {
  it("should have tests", () => {
    assert.ok(true)
  })
})
